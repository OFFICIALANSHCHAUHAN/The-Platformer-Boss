const http = require('http');
const WebSocket = require('ws');
const PORT = process.env.PORT || 3000;

const HTML = `<!DOCTYPE html><html><head><meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no"><title>The Platformer Boss</title><style>body{margin:0;background:linear-gradient(#0033A0,#00AEEF);overflow:hidden;font-family:Arial}canvas{display:block;background:linear-gradient(#87CEEB,#E0F6FF);touch-action:none}#hud{position:absolute;top:10px;width:100%;text-align:center;color:#fff;font-weight:bold;font-size:12px;text-shadow:1px 1px #000}#logo{position:absolute;top:10px;right:15px;color:gold;font-weight:bold;font-size:18px}#menu{position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);background:rgba(0,0,0,0.95);color:white;padding:15px;border-radius:15px;text-align:left;display:none;z-index:20;width:95%;max-width:420px;max-height:85%;overflow-y:auto}#menu h2{text-align:center;color:gold}#menu.player-box{border:1px solid #555;padding:8px;margin:5px;border-radius:8px;background:#222}#menu input,select{margin:3px;padding:4px;width:80px;background:#333;color:white;border:1px solid #555}#menu button{margin:3px;padding:6px 10px;font-size:12px;background:#E4002B;color:white;border:none;border-radius:6px}#namePrompt{position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);background:white;padding:20px;border-radius:15px;z-index:30;text-align:center}#win{position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);background:rgba(0,0,0,0.8);color:gold;padding:30px;border-radius:20px;font-size:28px;text-align:center;display:none;z-index:10}.btn{position:absolute;bottom:50px;width:70px;height:70px;background:rgba(255,255,255,0.2);border:3px solid white;border-radius:50%;color:white;font-size:35px;display:flex;align-items:center;justify-content:center;user-select:none}#left{left:15px}#right{left:100px}#jump{right:15px}</style></head><body><div id="hud">THE PLATFORMER BOSS - LOADING...</div><div id="logo">BOSS</div><div id="namePrompt"><h3>Enter Username</h3><input id="nameInput" placeholder="Your Name"><button onclick="setName()">Join</button></div><div id="menu"><h2>🔥 ADMIN MENU 🔥</h2><div id="playerControls"></div><button id="close">Close</button></div><div id="win"></div><canvas id="game"></canvas><div class="btn" id="left">←</div><div class="btn" id="right">→</div><div class="btn" id="jump">↑</div><audio id="coinSound" src="https://cdn.pixabay.com/audio/2025/04/16/audio_10fcf2d6c8.mp3"></audio><audio id="winSound" src="https://cdn.pixabay.com/audio/2024/08/30/audio_0a7d1f1b2b.mp3"></audio><script>const canvas=document.getElementById('game');canvas.width=window.innerWidth;canvas.height=window.innerHeight-120;const ctx=canvas.getContext('2d');const ws=new WebSocket('wss://'+location.host);let myId=null;let myName="Player";let gameState={players:{},platforms:[],coin:{},boss:null,level:1};const keys={left:false,right:false,jump:false};let keyComboTimer=null;const coinSound=document.getElementById('coinSound');const winSound=document.getElementById('winSound');function setName(){myName=document.getElementById('nameInput').value||"Player";ws.send(JSON.stringify({type:'setname',name:myName}));document.getElementById('namePrompt').style.display='none'}ws.onmessage=(e)=>{const data=JSON.parse(e.data);if(data.type==='init')myId=data.id;if(data.type==='state'){if(data.coinCollected)coinSound.play();if(data.gameOver){winSound.play();document.getElementById('win').style.display='block';document.getElementById('win').innerHTML=\`🏆 WINNER: \${data.winner}! 🏆<br>Beat The Platformer Boss!<br><button onclick="location.reload()">Play Again</button>\`}gameState=data;updateMenu();let hud="THE PLATFORMER BOSS | LVL:"+data.level+"/50 ";for(let id in data.players){hud+=data.players[id].name+" L"+data.players[id].level+" "+data.players[id].score+"❤️ ";}document.getElementById('hud').innerText=hud}};function sendInput(){ws.send(JSON.stringify({type:'input',keys}))}['left','right','jump'].forEach(id=>{const btn=document.getElementById(id);btn.addEventListener('touchstart',()=>{keys[id]=true;checkCombo();sendInput()});btn.addEventListener('touchend',()=>{keys[id]=false;sendInput()})});canvas.addEventListener('touchstart',(e)=>{if(document.getElementById('menu').style.display==='block'){let rect=canvas.getBoundingClientRect();let x=e.touches[0].clientX-rect.left;let y=e.touches[0].clientY-rect.top;ws.send(JSON.stringify({type:'cheat',action:'teleport',x,y}))}});function checkCombo(){if(keys.left&&keys.right&&keys.jump){clearTimeout(keyComboTimer);keyComboTimer=setTimeout(()=>{document.getElementById('menu').style.display='block'},200)}}function updateMenu(){let html="";for(let id in gameState.players){let p=gameState.players[id];html+=\`<div class="player-box"><b>\${p.name} \${id===myId?'(YOU)':''}</b><br>LVL:<input type="number" id="lvl_\${id}" value="\${p.level}" min="1" max="50"> HR:<input type="number" id="hr_\${id}" value="\${p.score}" min="0"> SPD:<input type="number" id="spd_\${id}" value="\${p.speed}" min="1" max="5" step="0.5"><br><button onclick="apply('\${id}')">Apply</button><button onclick="kill('\${id}')">Steal Heart</button><button onclick="give('\${id}')">Give Heart</button><br><label><input type="checkbox" id="fly_\${id}" \${p.fly?'checked':''} onchange="fly('\${id}')"> Fly</label><label><input type="checkbox" id="god_\${id}" \${p.god?'checked':''} onchange="god('\${id}')"> God</label><button onclick="skip('\${id}')">Skip LVL</button><button onclick="kick('\${id}')">Kick</button></div>\`}html+=\`<div class="player-box"><b>GLOBAL</b><br><button onclick="spawnCoin()">Spawn Coin</button><button onclick="setBossHP()">Boss HP:5</button></div>\`;document.getElementById('playerControls').innerHTML=html}function apply(id){ws.send(JSON.stringify({type:'cheat',action:'set',target:id,level:parseInt(document.getElementById('lvl_'+id).value),hearts:parseInt(document.getElementById('hr_'+id).value),speed:parseFloat(document.getElementById('spd_'+id).value)}))}function kill(t){ws.send(JSON.stringify({type:'cheat',action:'kill',target:t}))}function give(t){let a=prompt("Hearts to give?");if(a)ws.send(JSON.stringify({type:'cheat',action:'give',target:t,amount:parseInt(a)}))}function fly(t){ws.send(JSON.stringify({type:'cheat',action:'fly',target:t,value:document.getElementById('fly_'+t).checked}))}function god(t){ws.send(JSON.stringify({type:'cheat',action:'god',target:t,value:document.getElementById('god_'+t).checked}))}function skip(t){ws.send(JSON.stringify({type:'cheat',action:'skip',target:t}))}function kick(t){ws.send(JSON.stringify({type:'cheat',action:'kick',target:t}))}function spawnCoin(){ws.send(JSON.stringify({type:'cheat',action:'spawncoin'}))}function setBossHP(){ws.send(JSON.stringify({type:'cheat',action:'bossHP',hp:5}))}document.getElementById('close').onclick=()=>{document.getElementById('menu').style.display='none'};function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#2F3542';gameState.platforms.forEach(p=>ctx.fillRect(p.x,p.y,p.w,p.h));if(gameState.boss){ctx.fillStyle='#E4002B';ctx.fillRect(gameState.boss.x,gameState.boss.y,gameState.boss.w,gameState.boss.h);ctx.fillStyle='white';ctx.font='bold 14px Arial';ctx.fillText('BOSS:'+gameState.boss.hp,gameState.boss.x,gameState.boss.y-5)}ctx.fillStyle='gold';ctx.beginPath();ctx.arc(gameState.coin.x,gameState.coin.y,gameState.coin.r,0,Math.PI*2);ctx.fill();for(let id in gameState.players){let p=gameState.players[id];ctx.fillStyle=p.color;ctx.fillRect(p.x,p.y,p.w,p.h);ctx.fillStyle='white';ctx.font='12px Arial';ctx.fillText(p.name,p.x,p.y-5);if(p.fly){ctx.fillStyle='cyan';ctx.fillText('FLY',p.x,p.y-18)}if(p.god){ctx.fillStyle='gold';ctx.fillText('GOD',p.x+25,p.y-18)}}requestAnimationFrame(draw)}draw();</script></body></html>`;

const server = http.createServer((req, res) => {res.writeHead(200,{'Content-Type':'text/html'});res.end(HTML);});
const wss = new WebSocket.Server({ server });
const GRAVITY = 0.5; const MAX_LEVELS = 50; const BOSS_LEVELS = [10,20,30,40,50];
let players = {}; let level = 1; let coin = {x:400,y:250,r:12}; let boss = null; let gameOver = false; let winner = null;

function generateLevel(lvl) {
  let plats = [{x:0,y:560,w:800,h:40}];
  let platCount = 3 + Math.floor(lvl / 5);
  for(let i=0; i<platCount; i++){plats.push({x:Math.random()*650+50,y:500-(i*80)-(lvl*2),w:Math.max(60,120-lvl),h:20})}
  if(BOSS_LEVELS.includes(lvl)){boss={x:350,y:200,w:100,h:80,hp:5};coin.y=150;}else{boss=null;coin.x=Math.random()*700+50;coin.y=Math.random()*300+100;}
  return plats;
}
let platforms = generateLevel(level);
function rectCollision(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y}

function updatePhysics(){
  if(gameOver) return; let coinCollected=false; let bossDefeated=false;
  for(let id in players){
    let p=players[id]; let speed=p.speed||1;
    if(p.fly){p.vy=0; if(p.keys.left)p.x-=5*speed; if(p.keys.right)p.x+=5*speed; if(p.keys.jump)p.y-=5*speed;}
    else{if(p.keys.left)p.vx=-5*speed; else if(p.keys.right)p.vx=5*speed; else p.vx=0;if(p.keys.jump&&p.onGround){p.vy=-12*speed; p.onGround=false}p.vy+=GRAVITY; p.x+=p.vx; p.y+=p.vy; p.onGround=false;for(let plat of platforms){if(rectCollision(p,plat)&&p.vy>0){p.y=plat.y-p.h; p.vy=0; p.onGround=true}}if(p.y>600&&!p.god){p.score=Math.max(0,p.score-1); p.x=50; p.y=500}}
    if(boss&&rectCollision(p,boss)&&p.vy>0&&p.y<boss.y){boss.hp--; p.vy=-10; bossDefeated=true; if(boss.hp<=0){for(let pid in players)players[pid].level++; platforms=generateLevel(level+1); if(level>=MAX_LEVELS){gameOver=true; winner=p.name}}}
    let dx=p.x+p.w/2-coin.x; let dy=p.y+p.h/2-coin.y; if(Math.sqrt(dx*dx+dy*dy)<coin.r+p.w/2&&!boss){p.score++; coinCollected=true; coin.x=Math.random()*700+50; coin.y=Math.random()*300+100;}
    if(p.x<0)p.x=0; if(p.x>800-p.w)p.x=800-p.w;
  }
  wss.clients.forEach(client=>{if(client.readyState===WebSocket.OPEN){client.send(JSON.stringify({type:'state',players,platforms,coin,boss,level,isBossLevel:BOSS_LEVELS.includes(level),coinCollected,bossDefeated,gameOver,winner}))}})
}

wss.on('connection',ws=>{
  const id='p'+(Object.keys(players).length+1); const colors=['#E4002B','#00AEEF','#00FF00','#FFD700'];
  players[id]={x:id==='p1'?50:700,y:500,w:30,h:40,vx:0,vy:0,color:colors[Object.keys(players).length-1],keys:{left:false,right:false,jump:false},score:3,level:1,onGround:false,fly:false,god:false,speed:1,name:"Player"};
  ws.id=id; ws.send(JSON.stringify({type:'init',id}));
  ws.on('message',msg=>{
    const data=JSON.parse(msg);
    if(data.type==='setname'&&players[id])players[id].name=data.name;
    if(data.type==='input'&&players[id])players[id].keys=data.keys;
    if(data.type==='cheat'){
      if(data.action==='kill'&&players[data.target]&&players[id]&&players[data.target].score>0){players[data.target].score--; players[id].score++}
      if(data.action==='give'&&players[data.target])players[data.target].score+=data.amount;
      if(data.action==='fly'&&players[data.target])players[data.target].fly=data.value;
      if(data.action==='god'&&players[data.target])players[data.target].god=data.value;
      if(data.action==='set'&&players[data.target]){players[data.target].level=Math.min(50,Math.max(1,data.level)); players[data.target].score=Math.max(0,data.hearts); players[data.target].speed=data.speed;}
      if(data.action==='skip'&&players[data.target]){players[data.target].level++; platforms=generateLevel(players[data.target].level)}
      if(data.action==='kick'&&players[data.target])delete players[data.target];
      if(data.action==='spawncoin'){coin.x=Math.random()*700+50; coin.y=Math.random()*300+100}
      if(data.action==='teleport'&&players[id]){players[id].x=data.x; players[id].y=data.y}
      if(data.action==='bossHP'&&boss)boss.hp=data.hp;
    }
  });
  ws.on('close',()=>{delete players[id]});
});

setInterval(updatePhysics,1000/60); server.listen(PORT,()=>console.log('The Platformer Boss running on '+PORT))
